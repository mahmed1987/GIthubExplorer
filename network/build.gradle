android{
    buildTypes {
        release {
            buildConfigField "String", "API_ENDPOINT", '"https://api.github.com/"'
        }

        debug {
            buildConfigField "String", "API_ENDPOINT", '"https://api.github.com/"'
        }
    }
}