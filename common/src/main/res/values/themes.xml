<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GithubExplorer" parent="Theme.Material.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/ghost_white</item>
        <item name="android:statusBarColor">@color/ghost_white</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <style name="Theme.Material.DayNight.NoActionBar" parent="@android:style/Theme.Material.Light.NoActionBar" />

</resources>